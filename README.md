# Description
It installs slipstream on server and client side

# Requirements
Ubuntu 24.04

# How to use
```
bash install.sh [server|client] client-port resolver-IP:resolver-Port domain dns-server

example:
    bash install.sh server 7000 172.16.2.69:53 google.com 1.1.1.1
```
